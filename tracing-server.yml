server:
  port: 9411

---
spring:
  config:
    activate:
      on-profile: default
eureka:
  instance:
    # enable to register multiple app instances with a random server port
    instance-id: ${spring.application.name}:${random.uuid}

eureka:
  client:
    serviceUrl:
      defaultZone: http://petclinic-config-server-service:8761/eureka/
